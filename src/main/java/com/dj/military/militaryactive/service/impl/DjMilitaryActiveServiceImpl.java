package com.dj.military.militaryactive.service.impl;
import com.dj.military.militaryactive.service.DjMilitaryActiveServiceI;
import org.jeecgframework.core.common.service.impl.CommonServiceImpl;
import com.dj.military.militaryactive.entity.DjMilitaryActiveEntity;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import java.util.UUID;
import java.io.Serializable;

@Service("djMilitaryActiveService")
@Transactional
public class DjMilitaryActiveServiceImpl extends CommonServiceImpl implements DjMilitaryActiveServiceI {

	
 	public <T> void delete(T entity) {
 		super.delete(entity);
 		//执行删除操作配置的sql增强
		this.doDelSql((DjMilitaryActiveEntity)entity);
 	}
 	
 	public <T> Serializable save(T entity) {
 		Serializable t = super.save(entity);
 		//执行新增操作配置的sql增强
 		this.doAddSql((DjMilitaryActiveEntity)entity);
 		return t;
 	}
 	
 	public <T> void saveOrUpdate(T entity) {
 		super.saveOrUpdate(entity);
 		//执行更新操作配置的sql增强
 		this.doUpdateSql((DjMilitaryActiveEntity)entity);
 	}
 	
 	/**
	 * 默认按钮-sql增强-新增操作
	 * @param id
	 * @return
	 */
 	public boolean doAddSql(DjMilitaryActiveEntity t){
	 	return true;
 	}
 	/**
	 * 默认按钮-sql增强-更新操作
	 * @param id
	 * @return
	 */
 	public boolean doUpdateSql(DjMilitaryActiveEntity t){
	 	return true;
 	}
 	/**
	 * 默认按钮-sql增强-删除操作
	 * @param id
	 * @return
	 */
 	public boolean doDelSql(DjMilitaryActiveEntity t){
	 	return true;
 	}
 	
 	/**
	 * 替换sql中的变量
	 * @param sql
	 * @return
	 */
 	public String replaceVal(String sql,DjMilitaryActiveEntity t){
 		sql  = sql.replace("#{id}",String.valueOf(t.getId()));
 		sql  = sql.replace("#{create_name}",String.valueOf(t.getCreateName()));
 		sql  = sql.replace("#{create_by}",String.valueOf(t.getCreateBy()));
 		sql  = sql.replace("#{create_date}",String.valueOf(t.getCreateDate()));
 		sql  = sql.replace("#{update_name}",String.valueOf(t.getUpdateName()));
 		sql  = sql.replace("#{update_by}",String.valueOf(t.getUpdateBy()));
 		sql  = sql.replace("#{update_date}",String.valueOf(t.getUpdateDate()));
 		sql  = sql.replace("#{name}",String.valueOf(t.getName()));
 		sql  = sql.replace("#{sex}",String.valueOf(t.getSex()));
 		sql  = sql.replace("#{id_type}",String.valueOf(t.getIdType()));
 		sql  = sql.replace("#{id_no}",String.valueOf(t.getIdNo()));
 		sql  = sql.replace("#{degree}",String.valueOf(t.getDegree()));
 		sql  = sql.replace("#{permanent_address}",String.valueOf(t.getPermanentAddress()));
 		sql  = sql.replace("#{current_address}",String.valueOf(t.getCurrentAddress()));
 		sql  = sql.replace("#{draft_time}",String.valueOf(t.getDraftTime()));
 		sql  = sql.replace("#{health_exam_time}",String.valueOf(t.getHealthExamTime()));
 		sql  = sql.replace("#{politics_time}",String.valueOf(t.getPoliticsTime()));
 		sql  = sql.replace("#{military_time}",String.valueOf(t.getMilitaryTime()));
 		sql  = sql.replace("#{military_age_limit}",String.valueOf(t.getMilitaryAgeLimit()));
 		sql  = sql.replace("#{soldier_type}",String.valueOf(t.getSoldierType()));
 		sql  = sql.replace("#{military_type}",String.valueOf(t.getMilitaryType()));
 		sql  = sql.replace("#{military_address}",String.valueOf(t.getMilitaryAddress()));
 		sql  = sql.replace("#{remark}",String.valueOf(t.getRemark()));
 		sql  = sql.replace("#{UUID}",UUID.randomUUID().toString());
 		return sql;
 	}
}